# Local Vite dev can use /api because vite.config.js proxies it to FastAPI.
# In production/static hosting, set this to your deployed HTTPS backend URL,
# for example: https://agrigenius-api.onrender.com/api
VITE_API_BASE_URL=/api
